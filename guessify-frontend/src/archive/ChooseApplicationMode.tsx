import { ApplicationMode } from "../enums/application_mode.enum";

/*export type ChooseApplicationModePageProps = {
   setMode: (mode: ApplicationMode) => void;
};
export function ChooseApplicationModePage({
   setMode,
}: ChooseApplicationModePageProps) {
   return (
      <div>
         <h1>Welcome to Guessify! </h1>
         <p>Log in or continue as a guest</p>
         <button onClick={(e) => setMode(ApplicationMode.User)}>Login</button>
         <button onClick={(e) => setMode(ApplicationMode.Guest)}>Guest</button>
      </div>
   );
}*/
